apiVersion: v1
kind: Secret
type: kubernetes.io/opaque
metadata:
  name: fluentd-cloudwatch
  namespace: kube-system
  labels:
    app: fluentd-cloudwatch
    env: system
data:
  AWS_ACCESS_KEY_ID: {{ .Values.aws.id  | b64enc }}
  AWS_SECRET_ACCESS_KEY: {{ .Values.aws.secret | b64enc }}